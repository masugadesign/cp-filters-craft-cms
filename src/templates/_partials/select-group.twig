{#
Parameters:
	typeKey : entries | assets | categories | users | tags
	groupOptions : An array of entry types, asset volumes, user/categories/tag groups.
	groupId : The ID of whichever group is currently selected.
#}

{% import "_includes/forms" as forms %}

{% switch typeKey %}
	{% case 'entries' %}
		{% set groupLabel = "Entry Type" %}
		{% set groupInstructions = "Select an Entry Type to filter." %}
	{% case 'assets' %}
		{% set groupLabel = "Asset Volume" %}
		{% set groupInstructions = "Select an Asset Volume to filter." %}
	{% case 'users' %}
		{% set groupLabel = "User Group" %}
		{% set groupInstructions = "Select a User Group to filter." %}
	{% case 'categories' %}
		{% set groupLabel = "Category Group" %}
		{% set groupInstructions = "Select a Category Group to filter." %}
	{% case 'tags' %}
		{% set groupLabel = "Tag Group" %}
		{% set groupInstructions = "Select a Tag Group to filter." %}
{% endswitch %}

{{ forms.selectField({
	label: groupLabel,
	instructions: groupInstructions,
	id: "groupId",
	name: "groupId",
	options: groupOptions,
	value: groupId,
	required : false
}) }}
